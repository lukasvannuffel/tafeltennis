{% extends "index.twig" %}

{% requireLogin %}

{% block title %}Mijn Rekening - HNO Assenede{% endblock %}

{% block content %}

{% block styles %}
<link rel="stylesheet" href="{{ siteUrl }}css/style.css">
{% endblock %}

{# Geuploade bannerimage tonen #}
{% set image = entry.image.one() %}
<header class="relative text-white" style="background-image: url('{{ image.url }}'); background-size: cover; background-position: center;">
  <div class="bg-blue-900 bg-opacity-80 absolute inset-0"></div>
  <div class="relative z-10 max-w-5xl mx-auto py-20 px-8 text-center">
    <h1 class="text-4xl md:text-5xl font-bold mb-4">{{ entry.title }}</h1>
    <p class="text-lg md:text-xl mb-6">{{ entry.subtitle }}</p>
  </div>
</header>

{# Je inhoud hier toevoegen #}
<div class="rekening-content">
  {# Dit gedeelte kan worden ingevuld met de gegevens die je wilt tonen #}
  <h2>Rekeningen Details</h2>
  {# Voorbeeld voor het ophalen van rekeningen - commentaar als placeholder #}
  {# {% for rekening in craft.entries.section('rekeningen').all() %}
    <div class="rekening">
        <h2>{{ rekening.title }}</h2>
    </div>
  {% endfor %} #}
</div>

{% endblock %}
